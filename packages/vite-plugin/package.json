{
  "name": "@react-scan-cli/vite-plugin",
  "version": "0.1.6",
  "description": "Vite plugin for react-scan-cli — detects React re-render issues",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc --emitDeclarationOnly && bun build src/index.ts --outfile dist/index.mjs --format esm --target node --external vite && bun build src/index.ts --outfile dist/index.js --format cjs --target node --external vite",
    "dev": "bun build src/index.ts --outfile dist/index.mjs --format esm --target node --external vite --watch",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "react-scan": "latest"
  },
  "peerDependencies": {
    "vite": ">=4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "@types/bun": "latest"
  }
}
