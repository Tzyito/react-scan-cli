{
  "name": "react-scan-cli",
  "private": true,
  "workspaces": [
    "packages/*",
    "playground"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "dev": "bun run --filter '*' dev",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "eslint packages/*/src/**/*.ts",
    "verify:inject": "bun run scripts/verify-inject.ts",
    "verify:analyzer": "bun run scripts/verify-analyzer.ts",
    "verify:reporter": "bun run scripts/verify-reporter.ts",
    "verify": "bun run verify:inject && bun run verify:analyzer && bun run verify:reporter",
    "e2e": "bun run scripts/e2e.ts",
    "publish:all": "bun run build && npm publish --access public -w packages/vite-plugin && npm publish --access public -w packages/next && npm publish --access public -w packages/runner && npm publish --access public -w packages/skills"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "@types/bun": "latest"
  }
}
